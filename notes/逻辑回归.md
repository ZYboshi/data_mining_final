# 逻辑回归模型文档

## 📊 模型概述

逻辑回归是一种广泛用于二元分类问题的统计方法，通过逻辑函数将线性回归的输出映射到 [0,1] 区间，作为样本属于正类的概率。

### 核心特性
- **简单高效**：计算复杂度低，训练速度快
- **可解释性强**：提供特征系数，可直接解释特征影响
- **概率输出**：输出样本属于正类的概率而不仅仅是类别
- **正则化支持**：通过 L1/L2 正则化防止过拟合

## 🛠️ 代码实现


### 主要功能模块

1. **数据加载与预处理**
   ```python
   # 加载数据集
   data = pd.read_csv('./dataset/data_label_encoded.csv')
   X = data.drop('deposit', axis=1)  # 特征
   y = data['deposit']               # 目标变量
   
   # 标准化数值特征
   scaler = StandardScaler()
   X_scaled = X.copy()
   X_scaled[existing_cols] = scaler.fit_transform(X[existing_cols])

2. **十折交叉验证**
```python
# 创建模型
model = LogisticRegression(max_iter=1000, random_state=42)

# 创建分层10折交叉验证
cv = StratifiedKFold(n_splits=10, shuffle=True, random_state=42)
cv_scores = cross_val_score(model, X, y, cv=cv, scoring='accuracy')

```

3.特征重要性分析
```python
# 分析特征系数
feature_names = X.columns
coefficients = model.coef_[0]

# 创建特征重要性DataFrame
importance_df = pd.DataFrame({
    'feature': feature_names,
    'coefficient': coefficients,
    'abs_coefficient': np.abs(coefficients)
})

```
4. 模型训练与评估
```python
# 测试集评估
y_pred = model.predict(X_test)
y_proba = model.predict_proba(X_test)[:, 1]

# 输出评估结果
print(classification_report(y_test, y_pred))
print(f"AUC Score: {roc_auc_score(y_test, y_proba):.4f}")

```

##  性能指标
评估指标
* 准确率 (Accuracy)：正确预测的比例
* 精确率 (Precision)：预测为正的样本中真正为正的比例
* 召回率 (Recall)：实际为正的样本中被正确预测为正的比例
* F1 分数 (F1-Score)：精确率和召回率的调和平均
* AUC (Area Under Curve)：ROC曲线下面积，衡量模型整体分类能力


## 特征重要性分析

* 系数解释\
正系数：特征值增加 → 预测为"订阅存款"的概率增加\
负系数：特征值增加 → 预测为"订阅存款"的概率减少\
绝对值大小：特征的相对重要性

```text
Top 10 最重要的特征:
  特征            系数
  duration       1.2458   # 通话时长
  poutcome_success 1.1289  # 前期营销成功
  contact_unknown 0.9856   # 联系方式未知
  month_dec      -0.7564   # 十二月营销
  age            -0.5432   # 年龄

```
